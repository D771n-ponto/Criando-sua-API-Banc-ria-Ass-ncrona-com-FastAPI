version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: workout_db
    environment:
      POSTGRES_DB: workout
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"